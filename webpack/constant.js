const path = require('path');
const entryDir = path.join(__dirname, '../src');
const electromDirName = 'main';
const prodMode = 'production';

module.exports = {
  entryDir,
  electromDirName,
  prodMode,
};
